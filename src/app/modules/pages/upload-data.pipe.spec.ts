import { UploadDataPipe } from './upload-data.pipe';

describe('UploadDataPipe', () => {
  it('create an instance', () => {
    const pipe = new UploadDataPipe();
    expect(pipe).toBeTruthy();
  });
});
